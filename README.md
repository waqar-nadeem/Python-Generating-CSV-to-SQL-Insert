# Python-Generating-CSV-to-SQL-Insert
Converting CSV data into SQL INSERT statements using Python by reading CSV files, mapping columns to table fields, formatting values, handling nulls and special characters, and generating executable SQL scripts. Concepts used: CSV handling, file I/O, string formatting, data sanitization, and dictionary based processing.
